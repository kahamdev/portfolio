"use client";
import { useState } from "react";
const links = [["About", "/about"], ["Security practice", "/application-security"], ["Projects", "/projects"], ["Skills", "/skills"], ["Contact", "/contact"]] as const;
export function Header() { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur"><nav className="shell flex min-h-16 items-center justify-between" aria-label="Main navigation"><a className="font-bold tracking-tight text-navy" href="/">AR<span className="text-signal">.</span></a><button className="rounded p-2 md:hidden" onClick={() => setOpen(!open)} aria-expanded={open} aria-controls="menu">Menu</button><div id="menu" className={`${open ? "absolute left-0 top-16 block w-full border-b bg-white p-5" : "hidden"} items-center gap-6 text-sm font-semibold text-slate-600 md:static md:flex md:w-auto md:border-0 md:bg-transparent md:p-0`}>{links.map(([label, href]) => <a onClick={() => setOpen(false)} className="block py-2 hover:text-navy md:inline" href={href} key={href}>{label}</a>)}<a className="inline-block rounded-lg bg-navy px-4 py-2 text-white hover:bg-slate-800" href="/contact">Let&apos;s connect</a></div></nav></header>; }
