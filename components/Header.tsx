"use client";
import { useEffect, useState } from "react";
const links = [["About", "/about"], ["Security practice", "/application-security"], ["Projects", "/projects"], ["Skills", "/skills"], ["Contact", "/contact"]] as const;
export function Header() {
  const [open, setOpen] = useState(false);
  useEffect(() => { const closeOnEscape = (event: KeyboardEvent) => { if (event.key === "Escape") setOpen(false); }; window.addEventListener("keydown", closeOnEscape); return () => window.removeEventListener("keydown", closeOnEscape); }, []);
  return <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur"><nav className="shell flex min-h-16 items-center justify-between" aria-label="Main navigation"><a className="text-lg font-bold tracking-tight text-navy" href="/" aria-label="KahamDev portfolio home">Kaham<span className="text-signal">Dev</span></a><button className="inline-flex min-h-11 min-w-11 items-center justify-center rounded-lg text-navy hover:bg-slate-100 md:hidden" onClick={() => setOpen(value => !value)} aria-expanded={open} aria-controls="mobile-menu" aria-label={open ? "Close navigation menu" : "Open navigation menu"}><svg aria-hidden="true" className="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">{open ? <><path d="m6 6 12 12" /><path d="m18 6-12 12" /></> : <><path d="M4 6h16" /><path d="M4 12h16" /><path d="M4 18h16" /></>}</svg><span className="sr-only">{open ? "Close menu" : "Open menu"}</span></button><div className="hidden items-center gap-6 text-sm font-semibold text-slate-600 md:flex">{links.map(([label, href]) => <a className="hover:text-navy" href={href} key={href}>{label}</a>)}<a className="rounded-lg bg-navy px-4 py-2 text-white hover:bg-slate-800" href="/contact">Let&apos;s connect</a></div></nav><div id="mobile-menu" className={`${open ? "grid" : "hidden"} border-t border-slate-200 bg-white px-5 py-4 shadow-lg md:hidden`}><div className="shell grid gap-1 px-0">{links.map(([label, href]) => <a onClick={() => setOpen(false)} className="rounded-lg px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-100 hover:text-navy" href={href} key={href}>{label}</a>)}<a onClick={() => setOpen(false)} className="mt-2 rounded-lg bg-navy px-4 py-3 text-center text-sm font-bold text-white" href="/contact">Let&apos;s connect</a></div></div></header>;
}
