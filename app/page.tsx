import { ContactForm } from "@/components/ContactForm";
import { Header } from "@/components/Header";
import { domains, projects, skills } from "@/data/portfolio";

const lifecycle = ["Plan", "Code", "SAST / SCA", "Build", "Container / IaC", "DAST / IAST", "Deploy", "ASPM / Monitor"];
export default function Home() { return <><Header /><main id="top"><section className="overflow-hidden bg-navy pb-20 pt-20 text-white"><div className="shell grid items-center gap-12 lg:grid-cols-[1.2fr_.8fr]"><div><p className="eyebrow text-teal-300">Application security / DevSecOps</p><h1 className="mt-5 max-w-3xl text-5xl font-bold tracking-tight sm:text-6xl">Application Security Engineer</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-300">Securing applications across the software development lifecycle — from source code and dependencies to APIs, containers, Kubernetes and cloud infrastructure.</p><div className="mt-9 flex flex-wrap gap-3"><a href="#practice" className="rounded-lg bg-signal px-5 py-3 font-bold text-white hover:bg-teal-400">Explore security practice</a><a href="#contact" className="rounded-lg border border-slate-500 px-5 py-3 font-bold hover:border-white">Start a conversation</a></div></div><aside className="rounded-2xl border border-slate-600 bg-white/5 p-6"><p className="text-sm font-bold text-teal-300">SECURITY POSTURE · SAMPLE DATA</p><div className="mt-6 space-y-5">{[["Controls mapped", "42"], ["Pipeline checks", "8"], ["Security domains", "9"]].map(([label, value]) => <div className="flex items-end justify-between border-b border-slate-600 pb-3" key={label}><span className="text-slate-300">{label}</span><strong className="text-3xl">{value}</strong></div>)}</div><p className="mt-5 text-xs leading-5 text-slate-400">Illustrative dashboard values only; not production or client metrics.</p></aside></div></section>
<section id="about" className="shell py-20"><p className="eyebrow">About the portfolio</p><h2 className="section-title">Security that moves at the speed of delivery.</h2><p className="section-copy">This is a professional demonstration portfolio for an Application Security / DevSecOps Engineer. It focuses on pragmatic controls: high-signal findings, developer workflows, measurable remediation and defense in depth. Personal employment history and certifications have intentionally not been invented.</p></section>
<section id="practice" className="bg-mist py-20"><div className="shell"><p className="eyebrow">Interactive security model</p><h2 className="section-title">AppSec throughout the lifecycle</h2><p className="section-copy">Each stage produces evidence and feedback for the next. Select a practice area below for its role in the SDLC.</p><ol className="mt-10 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{lifecycle.map((phase, index) => <li className="rounded-xl border border-slate-200 bg-white p-4" key={phase}><span className="text-xs font-bold text-signal">0{index + 1}</span><p className="mt-1 font-bold text-navy">{phase}</p></li>)}</ol><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{domains.map(domain => <article className="card" key={domain.title}><div className="flex items-center justify-between"><h3 className="text-xl font-bold">{domain.title}</h3><span className="tag">{domain.phase}</span></div><p className="mt-3 leading-6 text-slate-600">{domain.summary}</p><p className="mt-4 text-sm"><b>Detects:</b> {domain.risks}</p><p className="mt-2 text-sm"><b>CI/CD:</b> {domain.workflow}</p><div className="mt-4 flex flex-wrap gap-2">{domain.tools.map(tool => <span className="tag" key={tool}>{tool}</span>)}</div></article>)}</div></div></section>
<section id="projects" className="shell py-20"><p className="eyebrow">Portfolio projects</p><h2 className="section-title">Six security-focused case-study frameworks</h2><p className="section-copy">Clearly labeled examples that illustrate a defensible approach; they do not claim real clients, findings, or outcomes.</p><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{projects.map(([title, label, summary, tools]) => <article className="card" key={title}><span className="tag">{label}</span><h3 className="mt-4 text-xl font-bold">{title}</h3><p className="mt-3 text-slate-600">{summary}</p><dl className="mt-5 space-y-2 text-sm"><div><dt className="font-bold">Methodology</dt><dd>Scope → assess → validate → remediate → verify</dd></div><div><dt className="font-bold">Tools</dt><dd>{tools.join(" · ")}</dd></div><div><dt className="font-bold">Learning focus</dt><dd>Risk-based prioritization and sustainable remediation.</dd></div></dl></article>)}</div></section>
<section id="skills" className="bg-navy py-20 text-white"><div className="shell"><p className="eyebrow text-teal-300">Skills matrix</p><h2 className="section-title text-white">Controls across code, cloud and runtime</h2><div className="mt-9 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{skills.map((skill, i) => <div className="rounded-xl border border-slate-600 bg-white/5 p-4" key={skill}><span className="text-xs text-teal-300">{i % 3 === 0 ? "Strategy" : i % 3 === 1 ? "Engineering" : "Automation"}</span><p className="mt-1 font-bold">{skill}</p></div>)}</div></div></section>
<section id="contact" className="shell grid gap-10 py-20 lg:grid-cols-2"><div><p className="eyebrow">Contact</p><h2 className="section-title">Build secure delivery together.</h2><p className="section-copy">This template is ready for a real contact integration when an approved endpoint and privacy approach are available. Until then, the form stays local by design.</p><p className="mt-6 text-sm leading-6 text-slate-500">For external links, always use HTTPS and <code>rel="noopener noreferrer"</code> with <code>target="_blank"</code>.</p></div><ContactForm /></section></main><footer className="border-t border-slate-200 py-8"><div className="shell flex flex-wrap justify-between gap-3 text-sm text-slate-500"><span>© {new Date().getFullYear()} Application Security Portfolio</span><span>Secure by design · Sample content clearly marked</span></div></footer></> }
